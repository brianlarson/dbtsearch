# DDEV config for stack-rewrite: MySQL 8+ (Craft CMS–recommended). Run Nuxt on your host.
# On main branch, legacy app uses Postgres; this branch uses MySQL for Vue/Nuxt + Craft alignment.
# See docs/ROADMAP.md and docs/GETTING-STARTED.md.

name: dbtsearch
type: php
docroot: .
database:
  type: mysql
  version: "8.0"
# Optional: fixed port for MySQL (default 3306). Uncomment if you need a specific port.
# host_db_port: "33060"
