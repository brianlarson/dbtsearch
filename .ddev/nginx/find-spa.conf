# Vue app lives at /find/ with history mode. Serve real files when present; otherwise
# the app shell so refresh and deep links work (Craft must not handle these URIs).
location = /find {
    return 301 /find/;
}

location ^~ /find/ {
    try_files $uri $uri/ /find/index.html;
}
