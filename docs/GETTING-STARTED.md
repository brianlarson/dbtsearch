# Getting started

This branch runs **Craft CMS** at the repo root (docroot `web/`).

Follow **[STACK-REWRITE-SETUP.md](STACK-REWRITE-SETUP.md)** for DDEV, `.env`, and first install.

The legacy React + Express + Postgres app lives on the `main` / `legacy` branches only.
